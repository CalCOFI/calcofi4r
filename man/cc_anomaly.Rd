% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transect.R
\name{cc_anomaly}
\alias{cc_anomaly}
\title{Join a section to a climatology and difference it}
\usage{
cc_anomaly(section, clim, stations)
}
\arguments{
\item{section}{from \code{\link[=cc_transect_section]{cc_transect_section()}}.}

\item{clim}{from \code{\link[=cc_climatology]{cc_climatology()}}.}

\item{stations}{from \code{\link[=cc_transect_stations]{cc_transect_stations()}} — supplies \code{grid_key} and the
month, which \code{section} does not carry.}
}
\value{
\code{section} plus \code{clim_mean}, \code{clim_sd}, \code{clim_n}, \code{anomaly},
\code{anomaly_sd}; \code{baseline} attribute carried through.
}
\description{
\code{anomaly = value - clim_mean}, matched on station, calendar month and depth
bin. Cells with no baseline come back \code{NA} rather than 0 — an unsampled
baseline is not a zero anomaly, and collapsing the two is how a map ends up
claiming "normal" for somewhere never measured.
}
\details{
\code{anomaly_sd} expresses the departure in baseline standard deviations, which
is what makes a 1 °C anomaly interpretable: large in the deep, unremarkable
at the surface in spring.
}
\concept{transect}
