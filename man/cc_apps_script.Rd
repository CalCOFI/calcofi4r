% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{cc_apps_script}
\alias{cc_apps_script}
\title{Apps Script source for the usage-log Sheet}
\usage{
cc_apps_script()
}
\value{
character scalar of JavaScript source
}
\description{
The \code{doPost()} handler to paste into the Sheet's bound Apps Script project.
It appends a \strong{batch} of rows in one \code{setValues()} call (the client sends
\verb{\{rows: [...]\}}), which is what keeps the write cost — and the Apps Script
execution quota — flat regardless of interaction rate.
}
\details{
Column order is taken from \code{\link[=cc_log_header]{cc_log_header()}}, so the Sheet, the script, and
the client payload cannot drift.
}
\examples{
cat(cc_apps_script())
}
\concept{analytics}
