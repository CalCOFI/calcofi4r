% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transect.R
\name{cc_bathy}
\alias{cc_bathy}
\title{GEBCO seafloor bathymetry over the CalCOFI area}
\source{
GEBCO Compilation Group (2025) GEBCO 2025 Grid,
\url{https://www.gebco.net/data_and_products/gridded_bathymetry_data/}.
}
\usage{
cc_bathy(path = NULL, cache_dir = NULL, refresh = FALSE)
}
\arguments{
\item{path}{explicit raster to load; defaults to the option / env var above,
then to the cached download.}

\item{cache_dir}{where to keep the download. Defaults to
\code{rappdirs::user_cache_dir("calcofi4r")}.}

\item{refresh}{re-download even if cached (default \code{FALSE}).}
}
\value{
A \code{terra::SpatRaster}, single layer \code{depth_m}.
}
\description{
A \code{terra::SpatRaster} of GEBCO 2025 sub-ice bathymetry cropped to the CalCOFI
grid, 15 arc-second (~390 m x 460 m at 33 degN), as \strong{positive-down depth in
metres with land clamped to 0}. Downloaded once and cached, so the first call
in a session costs a 4.3 MB fetch and later ones cost nothing.
}
\details{
The sign and land convention are baked into the published raster rather than
applied on read: a caller who sees \code{12.4} is 12.4 m under water, and \code{0} is
land. Nothing downstream has to remember to negate an elevation.
}
\section{Using a local file instead}{

Set \code{options(calcofi4r.bathy = "/path/to.tif")} (or the \code{CALCOFI_BATHY}
environment variable) to bypass the download — how an app that already ships
its own crop, or a machine with no network, keeps working.
}

\concept{transect}
