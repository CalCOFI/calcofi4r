% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transect.R
\name{cc_bathy_depth}
\alias{cc_bathy_depth}
\title{Seafloor depth at points}
\usage{
cc_bathy_depth(lon, lat, bathy = cc_bathy())
}
\arguments{
\item{lon, lat}{numeric vectors of equal length, decimal degrees.}

\item{bathy}{raster from \code{\link[=cc_bathy]{cc_bathy()}}; pass one explicitly to avoid re-reading
it per call.}
}
\value{
Numeric vector of depth in metres, \code{NA} outside the raster's extent.
Land reads 0, never negative. Positions outside the raster's extent
\strong{warn} with their count and bounding box rather than returning \code{NA} in
silence — the behaviour that let a quarter of the released positions read
as "no depth" without a line of output anywhere (D29, 2026-08-31).
}
\description{
Bilinear rather than nearest-cell, so a station on a steep slope is not
pinned to whichever 15 arc-second cell it happens to land in.
}
\concept{transect}
