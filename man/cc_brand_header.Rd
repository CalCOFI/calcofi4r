% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand.R
\name{cc_brand_header}
\alias{cc_brand_header}
\title{The brand header bar for a Shiny app}
\usage{
cc_brand_header(
  title,
  ...,
  subtitle = NULL,
  href = "./",
  toggle_id = "dark_toggle",
  mode = c("dark", "light"),
  brand_url = .CC_BRAND_URL
)
}
\arguments{
\item{title}{the app's name, shown beside the logo}

\item{...}{the app's own header controls (selects, buttons, help)}

\item{subtitle}{small muted text after the title (a release version, say)}

\item{href}{where the title links: the app's root}

\item{toggle_id}{id of the dark-mode switch (read it with \code{\link[=cc_is_dark]{cc_is_dark()}});
\code{NULL} for no switch}

\item{mode}{initial theme of the switch — pass \code{\link[=cc_theme]{cc_theme()}}\code{(request)}}

\item{brand_url}{where the assets live; the default is the only value a
published app should use}
}
\value{
a \verb{<header class="cc-header">} tag
}
\description{
The \code{.cc-header} chrome: CalCOFI logo far left linking to
\url{https://calcofi.io}, the app's \code{title} (linking to \code{href}, its own root),
a spacer, the app's own controls in \code{...}, and bslib's dark-mode switch.
}
\concept{brand}
