% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release_sources.R
\name{cc_catalog_views}
\alias{cc_catalog_views}
\alias{cc_view_tables}
\alias{cc_view_sql}
\title{Views a release catalog carries beside its tables}
\usage{
cc_catalog_views(catalog)

cc_view_tables(sql)

cc_view_sql(catalog, name, rp = function(table) paste0("\\"", table, "\\""))
}
\arguments{
\item{catalog}{a release catalog as returned by \code{\link[=cc_catalog]{cc_catalog()}} (either jsonlite form)}

\item{sql}{a view's SQL carrying \code{{{table}}} tokens}

\item{name}{the view's name}

\item{rp}{\verb{function(table) -> character(1)}}
}
\value{
\code{cc_catalog_views()}: a named list of SQL strings; \code{cc_view_tables()}: the distinct
table names in order of first appearance; \code{cc_view_sql()}: a length-one SQL string.
}
\description{
Since the v2026.09 releases (calcofi4db 3.31.0, pre-release plan D-S1) \code{catalog.json} may carry a
top-level \code{views} map: view name → SQL over \code{{{table}}} tokens, one token per table the view
reads. \code{obs} is the first: the UNION ALL over \code{obs_bio} and \code{obs_env} that reconstructs its 18
columns under their original names, so \verb{FROM obs} keeps working while the observation rows ship
once, as the pair. The table a view replaces is marked \code{deprecated} in \code{tables[]} (with
\code{replaced_by} and \code{removed_in}) for the release it still ships in.
}
\details{
\code{cc_catalog_views()} lists the views (an empty list for a catalog without any);
\code{cc_view_tables()} the tables one reads; \code{cc_view_sql()} its SQL with every token replaced by
\code{rp(table)} — a quoted identifier by default (the tables exist in the connection, as
\code{\link[=cc_get_db]{cc_get_db()}} arranges), or a \code{read_parquet(...)} from \code{\link[=cc_release_sources]{cc_release_sources()}} +
\code{\link[=cc_read_parquet_sql]{cc_read_parquet_sql()}} for a connection that has none. Wrap the result in parentheses to use
it in a \code{FROM}.
}
\examples{
\dontrun{
cat_ <- cc_catalog("latest")
names(cc_catalog_views(cat_))
rp  <- function(t) cc_read_parquet_sql(cc_release_sources(cat_, t))
sql <- paste0("SELECT count(*) FROM (", cc_view_sql(cat_, "obs", rp), ")")
}
}
\concept{database}
