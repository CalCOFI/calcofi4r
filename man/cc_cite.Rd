% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cite.R
\name{cc_cite}
\alias{cc_cite}
\title{Cite CalCOFI data}
\usage{
cc_cite(
  x = NULL,
  version = "latest",
  format = c("text", "bibtex", "csl"),
  con = NULL,
  resolve = FALSE
)
}
\arguments{
\item{x}{\code{NULL} (every dataset), a character vector of \code{dataset_key}, or a
data frame/tibble carrying a \code{dataset_key} column.}

\item{version}{release version (default \code{"latest"}). Only consulted for the
release-level citation (\code{cc_catalog(version)}) — with \code{con} supplied,
pass the version \code{con} was opened on if it is not \code{"latest"}, the same
caveat as \code{\link[=cc_describe_table]{cc_describe_table()}}.}

\item{format}{\code{"text"} (a character vector, release citation first),
\code{"bibtex"} (one string, every \verb{@misc\{...\}} entry concatenated) or \code{"csl"}
(a list of CSL-JSON items, one per entry).}

\item{con}{optional open connection from \code{\link[=cc_get_db]{cc_get_db()}}; when given it is
used as is (no new connection).}

\item{resolve}{\code{format = "bibtex"} only: fetch the DOI's own BibTeX from
\code{doi.org} for any entry with a DOI, instead of building it offline
(default \code{FALSE}).}
}
\value{
See \code{format}. The result carries a \code{source} attribute
(\code{"release"} or \code{"computed"}) describing where the release-level citation
came from.
}
\description{
Every CalCOFI release cites itself (\code{catalog.json}'s \code{citation}, set by
\code{calcofi4db::add_release_citation()}) and every dataset in it carries a
checked \code{citation_main}, a registered \code{license} and, where the source gives
one, a \code{doi} and \code{acknowledgement} (\code{calcofi4db::check_dataset_citation()},
calcofi4db >= 3.30.0, the attribution contract). \code{cc_cite()} is the one place
that formats them for a paper, a data-management plan or a \code{.bib} file — read
the \code{dataset} table off \code{con}, do not build a citation string by hand.
}
\details{
Every call returns the \strong{release citation first}, then one entry per
dataset. With \code{x = NULL} (default) that is every dataset in the release,
alphabetical by \code{dataset_key}; a character vector of \code{dataset_key} or a data
frame/tibble carrying a \code{dataset_key} column (so \code{cc_cite(cc_read_obs(...))}
works directly on a query result) cites just those, de-duplicated, in the
order given. A \code{dataset_key} that does not exist in the release is an error
naming it.

Each dataset entry always carries its \code{citation_main}; \code{format = "text"}
appends a \verb{License: <id>} line (plus the URL, for a \code{custom} license), a
\verb{DOI:} line when the dataset has one, an \verb{Acknowledgement:} line when the
source requires one, and (2026-09-05) always a \verb{Page:} line linking
\verb{https://calcofi.io/datasets/\{dataset_key\}/} — the dataset-catalog record's
own page (\code{\link[=cc_datasets]{cc_datasets()}}); the release citation gets the same line for
\verb{https://calcofi.io/datasets/release/}. \code{format = "bibtex"} and \code{format = "csl"} fold license and acknowledgement into one \code{note}/\code{note} field
instead, since neither format has a natural place for more than one, and do
not carry the page line.

\code{format = "bibtex"} builds every \verb{@misc\{...\}} entry \strong{offline}, from the
fields already on \code{dataset} and in the catalog — nothing here calls the
network by default. \code{resolve = TRUE} instead fetches
\verb{https://doi.org/<doi>} with \code{Accept: application/x-bibtex} for any entry
that has a DOI (falling back to the offline entry for one that does not, or
if the fetch fails), which is closer to what many reference managers expect
but is slower and requires a live connection.

A release frozen before the attribution contract (2026-09-03) carries no
\code{citation} in its catalog; \code{cc_cite()} computes the same wording
\code{calcofi4db::release_citation()} would have written (\code{source = "computed"}
on the result's \code{source} attribute, mirroring \code{\link[=cc_climatology]{cc_climatology()}}'s
\code{source}), rather than erroring or citing nothing.

The \strong{software} itself is cited separately — \code{citation("calcofi4r")} (from
\code{DESCRIPTION}'s \code{Authors@R}) for R, \code{calcofi4py.__citation__} for Python;
\code{cc_cite()} is for the \emph{data}.
}
\examples{
\dontrun{
cc_cite()
cc_cite("calcofi_dic")
cc_cite(format = "bibtex") |> cat()
cc_cite(cc_read_obs(datasets = "calcofi_dic"))
}
}
\concept{database}
