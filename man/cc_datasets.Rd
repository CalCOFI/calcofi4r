% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{cc_datasets}
\alias{cc_datasets}
\title{List CalCOFI datasets (the dataset-catalog record)}
\usage{
cc_datasets(
  version = "latest",
  what = c("datasets", "holdings", "reference"),
  base_https = "https://storage.googleapis.com/calcofi-db"
)
}
\arguments{
\item{version}{release version (default \code{"latest"})}

\item{what}{which table of the record to return: \code{"datasets"} (default, the 16+ integrated
datasets), \code{"holdings"} (datasets known but not yet in the database) or \code{"reference"} (cruises,
stations, spatial layers, bathymetry — not datasets, but linked the same way)}

\item{base_https}{https root of the bucket}
}
\value{
a \code{\link[tibble:tibble]{tibble::tibble()}}, one row per record
}
\description{
Reads a release's \code{datasets.json} — the record every dataset page, ERDDAP \code{infoUrl}, and
\code{cc_cite()}'s page line are built from (\code{calcofi4db::build_dataset_catalog()}, calcofi4db >=
4.1.0) — into a data frame, one row per record. \code{distributions}, \code{registrations}, \code{keywords},
\code{tables}, \code{objects} and \code{coverage$years}/\code{coverage$variables} arrive as list-columns (one list
per row); \code{provider}, \code{category}, \code{attribution}, \code{links}, \code{coverage}, \code{status} arrive as nested
data frames (\code{jsonlite}'s \code{simplifyVector} behavior) — \code{tidyr::unnest_wider()} flattens one when
needed.
}
\details{
A release frozen before the dataset catalog (calcofi4db < 4.1.0, before 2026-09) carries no
\code{datasets.json}; this errors naming the version rather than returning an empty table, so a
caller does not mistake "no catalog yet" for "no datasets".
}
\examples{
\dontrun{
ds <- cc_datasets()
ds[, c("dataset_key", "dataset_name_short")]
ds$links$page  # the dataset page URL, straight from the record
cc_datasets(what = "holdings")
}
}
\concept{database}
