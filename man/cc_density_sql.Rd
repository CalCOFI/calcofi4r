% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density.R
\name{cc_density_sql}
\alias{cc_density_sql}
\title{SQL deriving the two canonical densities and the effort class of a bio observation}
\usage{
cc_density_sql(
  alias = NULL,
  value = "measurement_value",
  units = "units",
  tow_type = "tow_type",
  std_haul_factor = "std_haul_factor",
  prop_sorted = "prop_sorted",
  volume_sampled_m3 = "volume_sampled_m3",
  as = TRUE
)
}
\arguments{
\item{alias}{table alias to prefix every column with (\code{"o"} -> \code{o.measurement_value}), or \code{NULL}.}

\item{value, units, tow_type, std_haul_factor, prop_sorted, volume_sampled_m3}{column names.}

\item{as}{\code{TRUE} returns one string of three \verb{expr AS name} clauses joined by \verb{,\\n}; \code{FALSE}
returns the three bare expressions as a named character vector.}
}
\value{
A single string (or a named character vector when \code{as = FALSE}).
}
\description{
Returns the \code{SELECT}-list fragment that derives \code{density_per_10m2}, \code{density_per_1000m3} and
\code{effort_class} from a count, its units, the gear and the effort of its own sample
(\code{std_haul_factor}, \code{prop_sorted}, \code{volume_sampled_m3}). The release cuts these columns onto
\code{obs_bio} with it; a consumer that joins \code{obs} to \code{sample_measurement} itself applies the same
expression. \code{calcofi4py::density_sql()} emits identical bytes.
}
\examples{
cat(cc_density_sql("o"))
}
\concept{quality}
