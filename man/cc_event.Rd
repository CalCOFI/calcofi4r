% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{cc_event}
\alias{cc_event}
\title{Build a tracking-event payload}
\usage{
cc_event(event, ...)
}
\arguments{
\item{event}{event name, e.g. \code{"map_query_sp"}; coerced to GA4-safe form}

\item{...}{named event parameters; \code{NULL}/\code{NA}/\code{""} are dropped and
multi-value parameters are collapsed to a comma-separated string}
}
\value{
list with \code{event} (character scalar), \code{params} (named list of
character) and \code{metrics} (named list of the reserved column values)
}
\description{
The pure payload constructor shared by \code{\link[=cc_track]{cc_track()}} and the tests. Normalises
the event name to GA4's rules (lowercase, \verb{[a-z0-9_]}, leading letter,
truncated to 40 characters) and drops \code{NULL}/\code{NA}/empty parameters so the
Sheet's \code{params} column stays readable.
}
\details{
The names in \code{\link[=cc_log_header]{cc_log_header()}} that have their own column — \code{n_rows}, \code{ms},
\code{status}, \code{error} — are \strong{reserved}: pass them like any other parameter and
they are hoisted out of \code{params} into \code{metrics}, keeping row counts and
durations numeric and chartable in the Sheet.

Parameter \emph{values} are NOT truncated here — the Sheet leg wants the full
string (a full species name, a list of taxa). The client truncates to 100
characters for the gtag leg only.
}
\examples{
cc_event("Map Query", taxa = "Sardinops sagax", n_rows = 1234, ms = 842)
cc_event("download", file = NULL, format = "csv")   # NULL dropped
}
\concept{analytics}
