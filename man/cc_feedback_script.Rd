% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{cc_feedback_script}
\alias{cc_feedback_script}
\title{Apps Script source for the feedback endpoint}
\usage{
cc_feedback_script(
  repos = c(explore = "CalCOFI/explore"),
  label = "feedback",
  max_per_hour = 20L,
  branch = "main"
)
}
\arguments{
\item{repos}{named character vector: which GitHub repository each \code{app}
files its public issue in (\code{c(explore = "CalCOFI/explore")}); an app not
listed here files no issue}

\item{label}{the issue label}

\item{max_per_hour}{the spam cap: submissions accepted per hour, across all
apps}

\item{branch}{the branch the screenshot is committed to (the raw URL the
issue embeds points at it)}
}
\value{
character scalar of JavaScript source
}
\description{
The \code{doPost()} handler to paste into the feedback Sheet's bound Apps Script
project (see the setup notes at the top of \code{R/feedback.R}). The client posts
one JSON object as \code{text/plain}:
\verb{\{app, url, release, viewport, theme, text, email, image (data:image/png;base64,…), website\}}
— \code{website} is the honeypot and must be empty.
}
\examples{
cat(cc_feedback_script())
}
\concept{analytics}
