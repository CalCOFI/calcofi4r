% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{cc_ga_html}
\alias{cc_ga_html}
\title{Write the analytics snippet to a standalone HTML file}
\usage{
cc_ga_html(
  path,
  app,
  content_group = app,
  app_version = "",
  measurement_id = .CC_GA_ID,
  log_url = ""
)
}
\arguments{
\item{path}{file to write, e.g. \code{"google-analytics.html"}}

\item{app}{short app id recorded on every event, e.g. \code{"db-viz-hex"}}

\item{content_group}{GA4 content group for reporting; defaults to \code{app}}

\item{app_version}{version string recorded on every event, e.g. a git SHA}

\item{measurement_id}{GA4 measurement ID; defaults to the CalCOFI apps property}

\item{log_url}{Apps Script \verb{/exec} endpoint. Defaults to \code{""} (GA4 only) —
unlike \code{\link[=cc_ga_js]{cc_ga_js()}}, a generated file does not read the environment, since
the result is committed to a repo.}
}
\value{
\code{path}, invisibly
}
\description{
For apps that do \strong{not} already depend on calcofi4r. Loading the calcofi4r
namespace costs several seconds of cold start (its imports pull in sf, terra,
stars, …), which a small app should not pay just to be counted — so generate
the snippet to a file and keep the app's existing
\code{includeHTML("google-analytics.html")}, with no runtime dependency at all.
}
\details{
Apps that already attach calcofi4r should call \code{\link[=cc_ga_head]{cc_ga_head()}} directly
instead: it costs them nothing and can never go stale.

The file is \strong{generated}. It carries a banner naming the exact call that
produced it, so when the shared snippet changes it is regenerated rather than
hand-patched in each repo.

A generated file is static, so it cannot carry the per-request \code{ip} of
\code{\link[=cc_ga_js]{cc_ga_js()}} — the Sheet leg is off by default here for that reason. GA4 is
unaffected: gtag resolves the client IP in the browser.
}
\examples{
f <- tempfile(fileext = ".html")
cc_ga_html(f, app = "marmam")
}
\concept{analytics}
