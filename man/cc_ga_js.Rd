% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{cc_ga_js}
\alias{cc_ga_js}
\title{Analytics \verb{<head>} snippet (GA4 + batched Sheet beacon)}
\usage{
cc_ga_js(
  app,
  content_group = app,
  app_version = "",
  ip = "",
  measurement_id = .CC_GA_ID,
  log_url = Sys.getenv("CALCOFI_LOG_URL", "")
)
}
\arguments{
\item{app}{short app id recorded on every event, e.g. \code{"db-viz-hex"}}

\item{content_group}{GA4 content group for reporting; defaults to \code{app}}

\item{app_version}{version string recorded on every event, e.g. a git SHA}

\item{ip}{client IP stamped on every logged row. Behind shiny-server this is
the \strong{only} way to record a real one: the websocket handshake the R session
sees is a fresh localhost connection with no \code{X-Forwarded-For}, so make the
app's \code{ui} a \verb{function(req)} and pass \code{cc_client_ip(req)} here. Left empty,
the \code{ip} column falls back to whatever \code{\link[=cc_track_session]{cc_track_session()}} reports
(\verb{127.0.0.1} behind a proxy).}

\item{measurement_id}{GA4 measurement ID; defaults to the CalCOFI apps property}

\item{log_url}{Apps Script \verb{/exec} endpoint for the Sheet log. Defaults to the
\code{CALCOFI_LOG_URL} environment variable; empty means the Sheet leg is a
silent no-op (GA4 still receives events).}
}
\value{
character scalar of raw HTML (\verb{<script>} tags)
}
\description{
Generates the self-contained HTML/JS installed once per page: the GA4 gtag
loader, the client-side event queue that beacons to the usage-log Sheet, the
\code{Shiny.addCustomMessageHandler()} handlers that \code{\link[=cc_track]{cc_track()}} and
\code{\link[=cc_track_session]{cc_track_session()}} depend on, and a \code{window.ccTrack(event, params, metrics)}
helper for page JS.
}
\details{
The same snippet serves every CalCOFI app; \code{content_group} (and the \code{app}
column of the Sheet) is what separates them in reporting, so no app needs its
own measurement ID.
}
\examples{
substr(cc_ga_js(app = "db-viz-hex"), 1, 60)
}
\concept{analytics}
