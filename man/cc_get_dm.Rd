% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{cc_get_dm}
\alias{cc_get_dm}
\title{Get CalCOFI Database as dm Object with Relationships}
\usage{
cc_get_dm(version = "latest", con = NULL)
}
\arguments{
\item{version}{Version string (e.g., "v2026.02") or "latest" (default)}

\item{con}{Optional existing DuckDB connection. If NULL (default),
calls \code{cc_get_db(version)} to create one.}
}
\value{
A \code{dm} object with PKs and FKs applied
}
\description{
Returns a \code{dm} object with primary keys and foreign keys applied from
the \code{relationships.json} sidecar file included in frozen releases.
This enables schema visualization via \code{dm::dm_draw()} and relationship-
aware operations.
}
\examples{
\dontrun{
dm <- cc_get_dm()
dm::dm_draw(dm, rankdir = "LR", view_type = "all")

# use existing connection
con <- cc_get_db()
dm <- cc_get_dm(con = con)
}
}
\concept{database}
