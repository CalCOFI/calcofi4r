% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate.R
\name{cc_interpolate}
\alias{cc_interpolate}
\title{Interpolate point values to a surface, exactly as the Explorer's Contours lens does}
\usage{
cc_interpolate(
  pts,
  method = c("ok", "idw", "tps"),
  cell_deg = 0.06,
  mask_km = 60,
  se = TRUE
)
}
\arguments{
\item{pts}{a data frame with \code{lon}, \code{lat} and \code{z} columns (\code{NA} rows are dropped).}

\item{method}{\code{"ok"} ordinary kriging (default; the kriging SD is the error surface), \code{"idw"}
inverse-distance weighting (power 1.3, radius 200 km, 5 km smoothing — no error surface), or \code{"tps"}
a thin-plate spline with the smoothing chosen by GCV (its standard error is the error surface).}

\item{cell_deg}{cell size in degrees of longitude (the Explorer uses 0.06).}

\item{mask_km}{cells farther than this from every point are \code{NA} (the Explorer uses 60).}

\item{se}{compute the error surface (\code{ok}, \code{tps}; always \code{NULL} for \code{idw}). It is the slow part.}
}
\value{
A list: \code{grid} (\code{lon0}, \code{lon1}, \code{lat_s}, \code{lat_n}, \code{nx}, \code{ny}, \code{cell_deg}), \code{values} (an \verb{ny x nx}
matrix, \strong{row 1 = north}), \code{se} (the same shape, or \code{NULL}), \code{fit} (\code{n}, \code{n_cells}, \code{loo} the leave-one-out
RMSE, \code{vg} the fitted variogram for \code{ok}, \code{edf} the effective degrees of freedom for \code{tps}), and \code{method}.
Turn it into a raster with \code{\link[=cc_interpolate_rast]{cc_interpolate_rast()}}.
}
\description{
The same algorithm as \code{calcofi.io/explore} (\code{lens=contour}) and \code{calcofi4py.interpolate()}, so a surface
drawn in R matches the map cell for cell: a grid of \code{cell_deg} degrees of longitude whose rows are evenly
spaced in Web-Mercator y (what the map stretches a bitmap over), a local equirectangular km frame for the
distances, and \strong{no value farther than \code{mask_km} from every point} — the surface never extrapolates.
Typically the points are the Explorer's station table (\code{grid_key} centres with a summary), i.e. what
\emph{Share → Download data} writes as \code{summary/station.csv}.
}
\examples{
set.seed(1)
pts <- data.frame(lon = runif(40, -122, -118), lat = runif(40, 31, 34))
pts$z <- 12 + 3 * sin(pts$lon + 122) + rnorm(40, 0, 0.3)
s <- cc_interpolate(pts, "ok", cell_deg = 0.25)
s$fit$loo; dim(s$values)
}
\seealso{
\code{\link[=cc_interpolate_rast]{cc_interpolate_rast()}}, \code{\link[=pts_to_rast_idw]{pts_to_rast_idw()}} (the superseded server-side IDW)
}
\concept{analyze}
