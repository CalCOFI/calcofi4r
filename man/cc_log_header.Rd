% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{cc_log_header}
\alias{cc_log_header}
\title{Column header for the usage-log Sheet}
\usage{
cc_log_header()
}
\value{
character vector of column names, in order
}
\description{
The exact first row the Google Sheet must carry for \code{\link[=cc_apps_script]{cc_apps_script()}} to
append into. Kept here so the Sheet, the Apps Script, and the client payload
cannot drift.
}
\details{
The first ten columns are the original \code{db-viz-hex} query log, so rows written
before the non-blocking rewrite keep their meaning; the rest were appended for
the browser-driven channel (\code{app} identifies which app wrote the row, so
several apps can share one Sheet).
}
\examples{
cc_log_header()
}
\concept{analytics}
