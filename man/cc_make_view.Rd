% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{cc_make_view}
\alias{cc_make_view}
\title{Create a Derived VIEW in the Database}
\usage{
cc_make_view(
  con,
  template = NULL,
  view_name = template,
  base_table = NULL,
  column_definitions = NULL
)
}
\arguments{
\item{con}{DBI connection to DuckDB}

\item{template}{Character. Name of a prebaked view template.
Available: "casts_extra". If provided, view_name and
column_definitions are taken from the template (but can be
overridden).}

\item{view_name}{Character. Name for the VIEW. Defaults to the
template name if using a template.}

\item{base_table}{Character. Base table name. Required if no
template is provided.}

\item{column_definitions}{Named character vector. Names are new
column names, values are DuckDB SQL expressions. Appended as
\verb{expression AS column_name} to \verb{SELECT *, ...}.}
}
\value{
A lazy dbplyr table reference to the created VIEW.
}
\description{
Creates a SQL VIEW with derived columns on top of base tables.
Supports prebaked templates (e.g., "casts_extra") or custom
column definitions specified as named SQL expressions.
}
\concept{database}
