% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{cc_match_bio_env}
\alias{cc_match_bio_env}
\title{Match biological to environmental observations}
\usage{
cc_match_bio_env(
  bio,
  env,
  max_dist_km = 2,
  max_time_hr = 6,
  join_method = c("nearest_time", "nearest_dist", "average"),
  con = NULL,
  version = "latest",
  collect = TRUE,
  return_sql = FALSE
)
}
\arguments{
\item{bio}{SQL \code{SELECT} string producing the biological side. \strong{Must} yield
columns \code{bio_id} (unique per observation), \code{bio_datetime} (\code{TIMESTAMP}),
\code{bio_lon}, \code{bio_lat} (decimal degrees) and \code{bio_value} (\code{DOUBLE}). Any
additional columns (e.g. \code{scientific_name}) are carried through to the
output as grouping keys.}

\item{env}{SQL \code{SELECT} string producing the environmental side. \strong{Must}
yield exactly \code{env_id}, \code{env_datetime} (\code{TIMESTAMP}), \code{env_lon}, \code{env_lat},
\code{env_value} (\code{DOUBLE}), \code{env_depth_m} (\code{DOUBLE}) and \code{measurement_type}.}

\item{max_dist_km}{Maximum match distance in kilometers (default: 2).}

\item{max_time_hr}{Maximum match time difference in hours (default: 6).}

\item{join_method}{One of \code{"nearest_time"} (default — keep the env
observation(s) closest in time, averaging ties), \code{"nearest_dist"} (closest
in space) or \code{"average"} (average every env observation in the window).}

\item{con}{Optional DuckDB connection. If \code{NULL} (default) a temporary
in-memory connection is created (and closed again when \code{collect = TRUE}).}

\item{version}{Release version string (e.g. \code{"v2026.05.14"}) or \code{"latest"}
(default). Recorded in the query metadata; the actual table URLs come from
\code{bio} / \code{env}.}

\item{collect}{If \code{TRUE} (default) execute and return a tibble; if \code{FALSE}
return a lazy \code{dplyr::tbl()} reference.}

\item{return_sql}{If \code{TRUE}, return the interpolated SQL string (with
\code{query_meta} attached) \strong{without executing}. Default: \code{FALSE}.}
}
\value{
When \code{return_sql = TRUE}, a length-1 character vector of SQL with
\code{attr(., "query_meta")}. Otherwise a tibble (or lazy \code{tbl}) of one row per
biological observation per \code{measurement_type}, with the matched
\code{env_value}, \code{env_value_sd}, \code{env_depth_m}, \code{n_env}, \code{dist_km} and
\code{time_diff_hr}, plus all \code{bio} columns. The result carries \code{attr(., "sql")}
and \code{attr(., "query_meta")} (package + release version, parameters and GCS
source URLs).
}
\description{
Core engine relating biological observations to environmental observations by
matching them on time and space. Builds a single DuckDB SQL string — a
temporal interval join (within \code{max_time_hr}) plus a spatial filter
(\code{ST_Distance_Sphere} within \code{max_dist_km}) — that reads directly from the
Parquet files of a frozen CalCOFI release on Google Cloud Storage, runs it,
and attaches the fully-interpolated SQL and query metadata as attributes.
}
\details{
Most users want a wrapper (\code{\link[=cc_match_ichthyo_by_name]{cc_match_ichthyo_by_name()}},
\code{\link[=cc_match_ichthyo_by_taxon]{cc_match_ichthyo_by_taxon()}} or \code{\link[=cc_match_zooplankton_biomass]{cc_match_zooplankton_biomass()}}) rather
than calling \code{cc_match_bio_env()} directly.
}
\examples{
\dontrun{
# build bio + env subqueries yourself, or use a wrapper
d <- cc_match_ichthyo_by_name("Sardinops sagax", env_var = "temperature")

# the exact, portable SQL that produced it
cat(attr(d, "sql"))
str(attr(d, "query_meta"))
}
}
\concept{match}
