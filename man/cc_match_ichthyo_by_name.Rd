% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{cc_match_ichthyo_by_name}
\alias{cc_match_ichthyo_by_name}
\title{Match ichthyoplankton to environmental data by scientific name}
\usage{
cc_match_ichthyo_by_name(
  scientific_name,
  env_var = "temperature",
  exact_match = TRUE,
  life_stage = NULL,
  date_min = NULL,
  date_max = NULL,
  depth_m_min = NULL,
  depth_m_max = NULL,
  max_dist_km = NULL,
  max_time_hr = NULL,
  relax_matching = FALSE,
  join_method = c("nearest_time", "nearest_dist", "average"),
  con = NULL,
  version = "latest",
  collect = TRUE,
  return_sql = FALSE
)
}
\arguments{
\item{scientific_name}{Character vector of scientific names to match against
\code{species.scientific_name}.}

\item{env_var}{Environmental \code{measurement_type} from the \code{bottle_measurement}
table (default: \code{"temperature"}). See \code{\link[=cc_list_measurement_types]{cc_list_measurement_types()}}.}

\item{exact_match}{If \code{TRUE} (default) match names exactly; if \code{FALSE} match
case-insensitively as substrings (\verb{ILIKE '\%name\%'}).}

\item{life_stage}{Optional character vector restricting \code{ichthyo.life_stage}
(e.g. \code{"larva"}, \code{"egg"}).}

\item{date_min, date_max}{Optional date bounds (\code{Date} or \code{"YYYY-MM-DD"}
string) on the tow start time.}

\item{depth_m_min, depth_m_max}{Optional depth bounds (meters) on the bottle
environmental observations.}

\item{max_dist_km, max_time_hr}{Match tolerances. Default to 2 km / 6 hr, or
5 km / 72 hr when \code{relax_matching = TRUE}; an explicit value always wins.}

\item{relax_matching}{Convenience flag mirroring the old API's
\code{relax_matching}: widens the default tolerances to 5 km / 72 hr.}

\item{join_method, con, version, collect, return_sql}{Passed to
\code{\link[=cc_match_bio_env]{cc_match_bio_env()}}.}
}
\value{
See \code{\link[=cc_match_bio_env]{cc_match_bio_env()}}.
}
\description{
Relates net-tow ichthyoplankton (fish egg / larva counts, standardized to
\code{std_tally}) to CTD-bottle environmental measurements, filtering the
biological side by scientific name. Supersedes the retired \verb{/ichthyodata}
Plumber endpoint.
}
\examples{
\dontrun{
# Pacific sardine larvae vs. temperature, Q1 2018, relaxed matching
# (note: CTD-bottle env data ends 2021-05, so Q1 2018 is used as the
#  recurring worked example across calcofi4r, int-app and the docs book)
d <- cc_match_ichthyo_by_name(
  "Sardinops sagax",
  env_var        = "temperature",
  life_stage     = "larva",
  date_min       = "2018-01-01",
  date_max       = "2018-03-31",
  relax_matching = TRUE)
}
}
\concept{match}
