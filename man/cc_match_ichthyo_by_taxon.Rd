% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{cc_match_ichthyo_by_taxon}
\alias{cc_match_ichthyo_by_taxon}
\title{Match ichthyoplankton to environmental data by WoRMS taxon subtree}
\usage{
cc_match_ichthyo_by_taxon(
  worms_id,
  env_var = "temperature",
  life_stage = NULL,
  date_min = NULL,
  date_max = NULL,
  depth_m_min = NULL,
  depth_m_max = NULL,
  max_dist_km = NULL,
  max_time_hr = NULL,
  relax_matching = FALSE,
  join_method = c("nearest_time", "nearest_dist", "average"),
  con = NULL,
  version = "latest",
  collect = TRUE,
  return_sql = FALSE
)
}
\arguments{
\item{worms_id}{Integer vector of WoRMS \code{taxonID}(s); the match includes each
id and every descendant taxon.}

\item{env_var}{Environmental \code{measurement_type} (default: \code{"temperature"}).}

\item{life_stage}{Optional character vector restricting \code{ichthyo.life_stage}.}

\item{date_min, date_max}{Optional date bounds on the tow start time.}

\item{depth_m_min, depth_m_max}{Optional depth bounds (meters) on the bottle
environmental observations.}

\item{max_dist_km, max_time_hr}{Match tolerances. Default to 2 km / 6 hr, or
5 km / 72 hr when \code{relax_matching = TRUE}; an explicit value always wins.}

\item{relax_matching}{Widen the default tolerances to 5 km / 72 hr.}

\item{join_method, con, version, collect, return_sql}{Passed to
\code{\link[=cc_match_bio_env]{cc_match_bio_env()}}.}
}
\value{
See \code{\link[=cc_match_bio_env]{cc_match_bio_env()}}.
}
\description{
Relates net-tow ichthyoplankton to CTD-bottle environmental measurements,
filtering the biological side to a taxon and all of its descendants. The
subtree is resolved with a recursive walk of \code{taxon.parentNameUsageID} over
the WoRMS authority. Supersedes the retired \verb{/itis_ichthyodata} endpoint,
replacing the dead ITIS \code{path} regex with the WoRMS \code{worms_id} hierarchy.
}
\examples{
\dontrun{
# Engraulis (anchovies) — WoRMS taxonID 125620 — and all descendants
d <- cc_match_ichthyo_by_taxon(125620, env_var = "salinity")
}
}
\concept{match}
