% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{cc_match_zooplankton_biomass}
\alias{cc_match_zooplankton_biomass}
\title{Match zooplankton biomass to environmental data}
\usage{
cc_match_zooplankton_biomass(
  env_var = "temperature",
  biomass_type = c("totalplankton", "smallplankton"),
  date_min = NULL,
  date_max = NULL,
  depth_m_min = NULL,
  depth_m_max = NULL,
  max_dist_km = NULL,
  max_time_hr = NULL,
  relax_matching = FALSE,
  join_method = c("nearest_time", "nearest_dist", "average"),
  con = NULL,
  version = "latest",
  collect = TRUE,
  return_sql = FALSE
)
}
\arguments{
\item{env_var}{Environmental \code{measurement_type} (default: \code{"temperature"}).}

\item{biomass_type}{Which net biomass column to use: \code{"totalplankton"}
(default) or \code{"smallplankton"}.}

\item{date_min, date_max}{Optional date bounds on the tow start time.}

\item{depth_m_min, depth_m_max}{Optional depth bounds (meters) on the bottle
environmental observations.}

\item{max_dist_km, max_time_hr}{Match tolerances. Default to 2 km / 6 hr, or
5 km / 72 hr when \code{relax_matching = TRUE}; an explicit value always wins.}

\item{relax_matching}{Widen the default tolerances to 5 km / 72 hr.}

\item{join_method, con, version, collect, return_sql}{Passed to
\code{\link[=cc_match_bio_env]{cc_match_bio_env()}}.}
}
\value{
See \code{\link[=cc_match_bio_env]{cc_match_bio_env()}}. \code{bio_value} is the selected biomass column.
}
\description{
Relates net-tow zooplankton displacement-volume biomass (\code{net.totalplankton}
or \code{net.smallplankton}) to CTD-bottle environmental measurements. Supersedes
the retired \verb{/zooplankton_biomass} endpoint.
}
\examples{
\dontrun{
d <- cc_match_zooplankton_biomass(env_var = "temperature")
}
}
\concept{match}
