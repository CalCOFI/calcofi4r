% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgres.R
\name{cc_pg_attach}
\alias{cc_pg_attach}
\title{Attach the CalCOFI PostgreSQL database inside a DuckDB connection}
\usage{
cc_pg_attach(
  con,
  alias = "pg",
  dbname = "calcofi",
  host = NULL,
  port = NULL,
  user = NULL,
  read_only = TRUE
)
}
\arguments{
\item{con}{a DuckDB connection, e.g. from \code{\link[=cc_get_db]{cc_get_db()}} or
\code{DBI::dbConnect(duckdb::duckdb())}}

\item{alias}{catalog name inside DuckDB; default \code{"pg"}}

\item{dbname}{database name; default \code{"calcofi"} (\code{"gis"} is the legacy 2022 db)}

\item{host}{host name; default described above}

\item{port}{port; default \code{5432} (\code{PGPORT} overrides; use \code{15432} if your
tunnel maps there)}

\item{user}{role name; default described above}

\item{read_only}{attach read-only (default \code{TRUE})}
}
\value{
\code{con} (invisibly)
}
\description{
Loads DuckDB's \code{postgres} extension and \code{ATTACH}es the PostgreSQL database,
so one DuckDB query can join the public release tables (from \code{\link[=cc_get_db]{cc_get_db()}})
with the team's PostgreSQL tables (\code{pg.ctd.flag}, \verb{pg.work.*}, …). Host /
port / user default exactly as in \code{\link[=cc_pg_connect]{cc_pg_connect()}}; the password is read by
libpq from \verb{~/.pgpass}.
}
\details{
With \code{read_only = FALSE} you can also write \emph{into} PostgreSQL from DuckDB
(\verb{INSERT INTO pg.work.my_table …}, \verb{CREATE TABLE pg.work.x AS SELECT …}),
which is how bulk loads from Parquet are done.
}
\examples{
\dontrun{
con <- cc_get_db()
cc_pg_attach(con)
DBI::dbGetQuery(con, "
  SELECT s.cruise_key, count(*) AS n_flags
  FROM pg.ctd.flag f JOIN sample s ON s.sample_key = f.sample_key
  GROUP BY 1 ORDER BY 2 DESC LIMIT 10")
}
}
\concept{database}
