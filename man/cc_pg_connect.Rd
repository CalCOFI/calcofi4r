% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgres.R
\name{cc_pg_connect}
\alias{cc_pg_connect}
\title{Connect to the CalCOFI PostgreSQL database}
\usage{
cc_pg_connect(
  dbname = "calcofi",
  host = NULL,
  port = NULL,
  user = NULL,
  password = NULL,
  tunnel = FALSE,
  ...
)
}
\arguments{
\item{dbname}{database name; default \code{"calcofi"} (\code{"gis"} is the legacy 2022 db)}

\item{host}{host name; default described above}

\item{port}{port; default \code{5432} (\code{PGPORT} overrides; use \code{15432} if your
tunnel maps there)}

\item{user}{role name; default described above}

\item{password}{password; default \code{NULL} = use \verb{~/.pgpass}}

\item{tunnel}{if \code{TRUE}, start an SSH tunnel with \code{\link[=cc_pg_tunnel]{cc_pg_tunnel()}} first
(only meaningful off-server)}

\item{...}{passed to \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}
}
\value{
a \code{DBI} connection; disconnect with \code{DBI::dbDisconnect()}
}
\description{
Opens a \code{DBI} connection (via \code{RPostgres}) to the multi-user CalCOFI
PostgreSQL database used by the CTD team for QA/QC (database \code{calcofi},
schemas \code{ctd} / \code{work} / your own). Every argument has a sensible default:
}
\details{
\itemize{
\item \strong{host}: \code{"postgis"} when running on the CalCOFI server (RStudio Server at
rstudio.calcofi.io, Shiny), otherwise \code{"localhost"} — i.e. the local end of
your SSH tunnel (\verb{ssh -N calcofi}, or \code{\link[=cc_pg_tunnel]{cc_pg_tunnel()}}). \code{PGHOST} overrides.
\item \strong{user}: \code{PGUSER} if set, else the role name found in your \verb{~/.pgpass} for
this host/port/database (the file you copied from the server), else your OS
user name.
\item \strong{password}: \code{NULL} — libpq reads \verb{~/.pgpass} (Windows:
\verb{\%APPDATA\%\\\\postgresql\\\\pgpass.conf}). Pass one only for throw-away use.
}
}
\examples{
\dontrun{
con <- cc_pg_connect()                  # tunnel already running, ~/.pgpass in place
DBI::dbListObjects(con, DBI::Id(schema = "ctd"))
dplyr::tbl(con, I("ctd.cast"))

con <- cc_pg_connect(tunnel = TRUE)     # also opens `ssh -N calcofi` for you
}
}
\seealso{
\code{\link[=cc_pg_tunnel]{cc_pg_tunnel()}}, \code{\link[=cc_pg_attach]{cc_pg_attach()}}, \code{\link[=cc_get_db]{cc_get_db()}} for the public releases
}
\concept{database}
