% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgres.R
\name{cc_pg_tunnel}
\alias{cc_pg_tunnel}
\alias{cc_pg_tunnel_close}
\title{Open an SSH tunnel to the CalCOFI server's PostgreSQL}
\usage{
cc_pg_tunnel(
  ssh_host = "calcofi",
  local_port = 5432L,
  remote_port = 5432L,
  wait = 10
)

cc_pg_tunnel_close(ssh_host = "calcofi", local_port = 5432L)
}
\arguments{
\item{ssh_host}{the \code{Host} alias from \verb{~/.ssh/config} (or \code{user@ssh.calcofi.io})}

\item{local_port}{local port to listen on; default \code{5432}}

\item{remote_port}{port on the server; default \code{5432}}

\item{wait}{seconds to wait for the port to come up; default \code{10}}
}
\value{
the \code{processx::process} (invisibly), or \code{NULL} if the port was
already open
}
\description{
Runs \verb{ssh -N -L <local_port>:localhost:<remote_port> <ssh_host>} in the
background with \code{processx}, so \code{cc_pg_connect()} can reach the database on
\verb{localhost:<local_port>}. Uses your \verb{~/.ssh/config} entry (the docs call it
\code{calcofi}: \verb{HostName ssh.calcofi.io}, \verb{User <you>}, \verb{IdentityFile …}), so no
credentials are handled here. Windows 10+ has \code{ssh.exe} built in.
}
\details{
The process is kept in a package-level registry and reused while alive; call
\code{\link[=cc_pg_tunnel_close]{cc_pg_tunnel_close()}} to stop it. If something already listens on
\code{local_port} (an earlier tunnel, a local Postgres) the port is left alone and
a message says so — set \code{local_port = 15432} in both places in that case.
}
\concept{database}
