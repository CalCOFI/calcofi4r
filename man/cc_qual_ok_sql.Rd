% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qual.R
\name{cc_qual_ok_sql}
\alias{cc_qual_ok_sql}
\title{SQL predicate keeping only observations whose quality flag is not bad}
\usage{
cc_qual_ok_sql(alias = NULL)
}
\arguments{
\item{alias}{Optional table alias to prefix the columns with (e.g. \code{"o"}).}
}
\value{
A single string.
}
\description{
Returns a boolean SQL fragment over \code{dataset_key} and \code{measurement_qual}
that is \code{TRUE} for unflagged rows (NULL), for datasets without a flag
vocabulary, and for codes not in \link{CC_QUAL_EXCLUDE}; \code{FALSE} for suspect /
bad / missing. Bottle codes were written as \code{"8.0"} through v2026.08.14, so
the trailing \code{.0} is stripped before comparison. Append it to any \code{WHERE}
over \code{obs}, \code{obs_ctd_full}, \code{sample_measurement} or \code{ctd_thin}.
}
\examples{
cc_qual_ok_sql("o")
}
\concept{quality}
