% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_core.R
\name{cc_read_ctd_full}
\alias{cc_read_ctd_full}
\title{Read full-resolution CTD scans (\code{obs_ctd_full}, supplemental)}
\usage{
cc_read_ctd_full(..., version = "latest", collect = TRUE)
}
\arguments{
\item{...}{\code{dplyr::filter()} expressions applied to \code{obs_ctd_full}.}

\item{version}{database version (default \code{"latest"}).}

\item{collect}{if \code{TRUE} (default) return a tibble; else the lazy \code{dplyr::tbl}.}
}
\value{
Tibble of full-resolution CTD observations or a lazy table.
}
\description{
The supplemental full per-scan CTD table (~216M rows), same schema as \code{obs}.
The default \code{obs} already carries CTD via the thinned \code{ctd_thin}; for
full-resolution work filter CTD out of \code{obs} and union \code{obs_ctd_full} — never
sum both (\code{ctd_thin} is a subset of \code{ctd_measurement}). Attached only when the
release catalog lists it; expect a large scan.
}
\concept{read}
