% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_core.R
\name{cc_read_obs}
\alias{cc_read_obs}
\title{Read consolidated observations (\code{obs})}
\usage{
cc_read_obs(
  ...,
  realm = NULL,
  datasets = NULL,
  measurement_types = NULL,
  version = "latest",
  collect = TRUE
)
}
\arguments{
\item{...}{\code{dplyr::filter()} expressions applied to \code{obs}.}

\item{realm}{optional \code{'env'} or \code{'bio'} to restrict the realm.}

\item{datasets}{optional vector of \code{dataset_key}s (e.g. \code{"calcofi_bottle"}).}

\item{measurement_types}{optional vector of \code{measurement_type}s.}

\item{version}{database version (default \code{"latest"}).}

\item{collect}{if \code{TRUE} (default) return a tibble; else the lazy \code{dplyr::tbl}.}
}
\value{
Tibble of observations (if \code{collect=TRUE}) or a lazy table.
}
\description{
The common occurrence-headline surface across every dataset: one scalar per
row, \code{realm} \code{'env'}/\code{'bio'}, provenance-stamped with \code{dataset_key}, keyed to
the sampling event via \code{sample_key}, and carrying \code{grid_key}/\code{cruise_key}/
\code{hex_id} for rollups. Environmental CTD is represented by the thinned
\code{ctd_thin}; use \code{\link[=cc_read_ctd_full]{cc_read_ctd_full()}} for full-resolution scans.
}
\examples{
\dontrun{
# larval abundance per station for one species
cc_read_obs(realm = "bio", measurement_types = "abundance",
            datasets = "swfsc_ichthyo")
}
}
\concept{read}
