% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_core.R
\name{cc_read_sample}
\alias{cc_read_sample}
\title{Read the sampling-event dimension (\code{sample})}
\usage{
cc_read_sample(
  ...,
  sample_types = NULL,
  datasets = NULL,
  version = "latest",
  collect = TRUE
)
}
\arguments{
\item{...}{\code{dplyr::filter()} expressions applied to \code{sample}.}

\item{sample_types}{optional vector of \code{sample_type}s.}

\item{datasets}{optional vector of \code{dataset_key}s.}

\item{version}{database version (default \code{"latest"}).}

\item{collect}{if \code{TRUE} (default) return a tibble; else the lazy \code{dplyr::tbl}.}
}
\value{
Tibble of sampling events (if \code{collect=TRUE}) or a lazy table.
}
\description{
One row per physical sampling event across every dataset, at its native grain
(\code{sample_type} = site/tow/net/cast/bottle/underway/transect/region_pool), with
\code{parent_sample_key} + \code{root_sample_key} encoding the \code{site->tow->net} and
\code{cast->bottle} hierarchies. Count sampling events with
\code{dplyr::count(sample_type)} or \verb{count(DISTINCT root_sample_key)}.
}
\concept{read}
