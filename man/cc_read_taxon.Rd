% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{cc_read_taxon}
\alias{cc_read_taxon}
\title{Read CalCOFI taxonomy}
\usage{
cc_read_taxon(..., version = "latest", collect = TRUE)
}
\arguments{
\item{...}{Additional filter expressions passed to \code{dplyr::filter()}}

\item{version}{Database version (default: "latest")}

\item{collect}{If TRUE, collect results into memory. If FALSE, return
lazy dbplyr table (default: TRUE)}
}
\value{
Tibble of taxa (if collect=TRUE) or lazy table
}
\description{
One row per taxon, keyed \verb{worms:<id>} or \verb{itis:<id>} (birds key ITIS, because
WoRMS bird taxonomy lags). Carries \code{worms_id} / \code{itis_id} / \code{gbif_id},
\code{parent_taxon_key} and the flattened classification, so a hierarchy rollup
("everything in Decapoda") resolves without a second source.
}
\details{
Replaces the per-dataset \code{species} table, which the taxon consolidation
removed — see \code{\link[=cc_read_species]{cc_read_species()}}.
}
\examples{
\dontrun{
taxa <- cc_read_taxon()
anchovy <- cc_read_taxon(scientific_name == "Engraulis mordax")
}
}
\concept{read}
