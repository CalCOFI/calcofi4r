% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand.R
\name{cc_release_chip}
\alias{cc_release_chip}
\title{The integrated-database release chip}
\usage{
cc_release_chip(version, href = cc_release_url(version))
}
\arguments{
\item{version}{release version, \code{"v2026.08.25"}; \code{NULL}/\code{NA}/\code{""} → no chip}

\item{href}{where the chip links; default \code{\link[=cc_release_url]{cc_release_url()}}}
}
\value{
an \verb{<a class="cc-release">} tag, or \code{NULL}
}
\description{
\verb{release <b>v2026.08.25</b>} in the brand header, right after the title — so it
survives a collapsed sidebar and every tab switch, and travels with a
screenshot. Links to \code{\link[=cc_release_url]{cc_release_url()}}. \code{\link[=cc_brand_header]{cc_brand_header()}} emits it from its
\code{release} argument; call this directly where a framework owns the bar
(\code{page_sidebar()}'s title, \code{page_navbar()}).
}
\details{
Show the release the page's data was \strong{built from} (a sidecar the app's
\code{prep_db.R} wrote), never "latest" fetched at load: the two diverge between a
release and the next redeploy, and a figure is only reproducible if the
release that produced it travelled with it.
}
\examples{
cc_release_chip("v2026.08.25")
}
\concept{brand}
