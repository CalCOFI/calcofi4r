% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release_sources.R
\name{cc_release_sources}
\alias{cc_release_sources}
\title{Resolve where a release table's parquet bytes live}
\usage{
cc_release_sources(
  catalog,
  table,
  base_https = "https://storage.googleapis.com/calcofi-db"
)
}
\arguments{
\item{catalog}{a release catalog as returned by \code{\link[=cc_catalog]{cc_catalog()}} (a list; a
\code{jsonlite}-simplified data frame form is accepted too)}

\item{table}{table name}

\item{base_https}{https root of the bucket (default the public GCS host)}
}
\value{
list with \code{urls} (character; https, or one \verb{s3://} glob for a legacy
partitioned table), \code{hive} (logical: pass \code{hive_partitioning = true}),
\code{canonical} (logical: from \code{objects[]}), \code{hashes} (content hashes, or \code{NA}),
\code{local_paths} (relative cache paths, one per url, content-addressed so a
table unchanged between releases is cached once) and \code{single_file} (the
whole-table file a partitioned table may also publish — \code{obs} does — for
https-only readers that cannot take a list; \code{NA} otherwise. Read one or the
other, never both)
}
\description{
Since the v2026.09 releases the database is \strong{content-addressed}: each table
(or each partition of a partitioned table) is one immutable object under
\verb{gs://calcofi-db/ducklake/tables/\{table\}/\{content_hash\}/…}, and a release's
\code{catalog.json} lists those objects per table in \code{objects[]}. A release that
changed three tables shares every other object with the release before it,
so a pinned consumer that has already fetched a table need not fetch it
again — and the archive can drop a retired version's own copies without
breaking anyone who reads through the catalog.
}
\details{
This is the one place a consumer turns a catalog entry into URLs. Read the
catalog with \code{\link[=cc_catalog]{cc_catalog()}} (or \code{jsonlite::fromJSON(simplifyVector = FALSE)})
and never build a \verb{releases/\{version\}/parquet/…} path by hand: that path is
only guaranteed to answer for the promoted and consolidated versions.

Rules, in order:
\enumerate{
\item entry has \code{objects[]} → one https URL per object, in catalog order
(partition files carry their \code{key=value} segment, so DuckDB's
\code{hive_partitioning = true} recovers the partition column from the URL);
\item otherwise (releases before v2026.09) → the legacy per-release path:
\verb{…/releases/\{version\}/parquet/\{table\}.parquet}, or an \verb{s3://} glob for a
partitioned table (DuckDB cannot glob over https).
}
}
\examples{
\dontrun{
src <- cc_release_sources(cc_catalog("latest"), "obs")
con <- DBI::dbConnect(duckdb::duckdb())
DBI::dbExecute(con, paste0("CREATE VIEW obs AS SELECT * FROM ", cc_read_parquet_sql(src)))
}
}
\concept{database}
