% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{cc_tbl}
\alias{cc_tbl}
\title{Access a CalCOFI Database Table}
\usage{
cc_tbl(con, table_name, layer = NULL, geom_col = "geom", crs = 4326)
}
\arguments{
\item{con}{DuckDB connection (from \code{\link[=cc_get_db]{cc_get_db()}})}

\item{table_name}{Name of the table}

\item{layer}{Required when \code{table_name = "_spatial"}. Character string
specifying which spatial layer to return.}

\item{geom_col}{Name of the geometry column for spatial tables
(default: "geom"). Use this to select alternate geometry columns,
e.g. \code{"geom_ctr"} for grid centroids.}

\item{crs}{CRS to assign to the returned sf object (default: 4326)}
}
\value{
For non-spatial tables: a lazy \code{dplyr::tbl()} reference.
For spatial tables: an \code{sf} object with geometry.
}
\description{
Unified interface for reading any table from the CalCOFI database.
Returns a lazy \code{dplyr::tbl()} reference for non-spatial tables, or
an \code{sf} object for tables with geometry columns.
}
\details{
For the \verb{_spatial} table, automatically pivots attributes from
\verb{_spatial_attr} wide and returns an sf object filtered to the
requested layer.
}
\examples{
\dontrun{
con <- cc_get_db()

# non-spatial: returns lazy dbplyr reference
cc_tbl(con, "ichthyo")

# spatial: returns sf with default geom column
cc_tbl(con, "grid")

# spatial: select alternate geometry
cc_tbl(con, "grid", geom_col = "geom_ctr")

# _spatial: returns sf with pivoted attributes for a layer
cc_tbl(con, "_spatial", layer = "CA Counties")
}
}
\concept{database}
