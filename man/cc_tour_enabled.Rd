% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand.R
\name{cc_tour_enabled}
\alias{cc_tour_enabled}
\title{Should the guided tour run? (\code{?tour=off})}
\usage{
cc_tour_enabled(query = NULL, session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{query}{a query string (\code{"?tour=off"}) or a parsed list; \code{NULL} reads the
session's URL via \code{\link[shiny:getQueryString]{shiny::getQueryString()}} (reactive context required)}

\item{session}{Shiny session, for the \code{NULL} case}
}
\value{
\code{TRUE} unless the URL switched the tour off
}
\description{
The contract's one URL parameter besides \verb{?theme=}: \code{tour=off|false|0|no}
suppresses a first-visit tour or welcome modal so a screenshot — or a
colleague following a link — sees the interface. Everything else (absent,
\code{on}, \code{1}, …) leaves the app's own first-visit logic in charge.
}
\examples{
cc_tour_enabled("?tour=off")
cc_tour_enabled("?cruise=2026-04-3322")
}
\concept{brand}
