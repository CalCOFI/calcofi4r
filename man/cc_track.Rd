% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{cc_track}
\alias{cc_track}
\title{Send a tracking event from the Shiny server to the browser}
\usage{
cc_track(session, event, ...)
}
\arguments{
\item{session}{the Shiny \code{session} object}

\item{event}{event name, passed to \code{\link[=cc_event]{cc_event()}}}

\item{...}{named event parameters, passed to \code{\link[=cc_event]{cc_event()}}; \code{n_rows}, \code{ms},
\code{status} and \code{error} land in their own Sheet columns}
}
\value{
the payload, invisibly
}
\description{
Pushes an event over the session's existing websocket; the client-side
handler installed by \code{\link[=cc_ga_js]{cc_ga_js()}} forwards it to GA4 and queues it for the
Sheet log. \strong{Non-blocking} — no HTTP request is made on the R side, so a slow
or unreachable log endpoint can never stall a reactive.
}
\details{
Use it for facts only the server knows (the taxa behind a picker value, a
query's row count, an error); pure UI interactions are better tracked
client-side with \code{window.ccTrack()} (see \code{\link[=cc_ga_js]{cc_ga_js()}}).
}
\examples{
\dontrun{
cc_track(session, "map_query_sp", taxa = sel_name, n_rows = 1234, ms = 842)
}
}
\concept{analytics}
