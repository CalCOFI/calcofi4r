% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{cc_track_query}
\alias{cc_track_query}
\title{Time a query, log it, and return its result}
\usage{
cc_track_query(session, event, params = list(), expr)
}
\arguments{
\item{session}{the Shiny \code{session} object}

\item{event}{event name, e.g. \code{"map_query_sp"}}

\item{params}{named list of query parameters (taxa, dates, depths, …)}

\item{expr}{the query expression; evaluated once, lazily}
}
\value{
the value of \code{expr}
}
\description{
Wraps a (usually lazy) query expression: times it, records the row count when
the result is a data frame, tracks the outcome via \code{\link[=cc_track]{cc_track()}}, and returns
the result unchanged. An error is tracked as \code{status = "error"} with its
message and then re-raised, so instrumentation changes nothing about the app's
behaviour.
}
\examples{
\dontrun{
df_sp <- cc_track_query(
  session, "map_query_sp", list(taxa = sel_name, quarters = sel_qtr),
  get_sp(sel_name, sel_qtr, sel_date_range, ck_children))
}
}
\concept{analytics}
