% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{cc_track_session}
\alias{cc_track_session}
\title{Hand the browser the session facts only the server knows}
\usage{
cc_track_session(session)
}
\arguments{
\item{session}{the Shiny \code{session} object}
}
\value{
the sent list, invisibly
}
\description{
The client IP and the Shiny session token cannot be read in JavaScript, so the
server pushes them once at session start; the client then stamps them on every
queued row (the \code{ip} and \code{session} columns of \code{\link[=cc_log_header]{cc_log_header()}}). Call it at
the top of the \code{server} function, before any \code{\link[=cc_track]{cc_track()}} call, so no event
is written without them.
}
\details{
The token is authoritative, the IP is only a \strong{fallback}: behind
shiny-server a session sees \verb{127.0.0.1}, so an \code{ip} already baked into the
page by \code{\link[=cc_ga_js]{cc_ga_js()}} wins and this one is ignored.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  cc_track_session(session)
  ...
}
}
}
\concept{analytics}
