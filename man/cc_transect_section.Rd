% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transect.R
\name{cc_transect_section}
\alias{cc_transect_section}
\title{Observations along a transect, binned by depth}
\usage{
cc_transect_section(
  con,
  line,
  cruise_key = NULL,
  variables = "temperature_ave",
  dataset_key = "calcofi_ctd-cast",
  depth_max = 500,
  depth_bin = 5,
  x = c("occupied", "line")
)
}
\arguments{
\item{con}{DuckDB connection to a release.}

\item{line, cruise_key, dataset_key}{as in \code{\link[=cc_transect_stations]{cc_transect_stations()}}.}

\item{variables}{\code{measurement_type}s to return.}

\item{depth_max}{deepest bin, m (default 500 — a handful of casts reach
5000 m, and letting them set the axis squashes every standard cast into the
top tenth of the plot).}

\item{depth_bin}{bin width, m (default 5).}

\item{x}{passed to \code{\link[=cc_transect_stations]{cc_transect_stations()}}.}
}
\value{
Tibble: \code{cruise_key}, \code{sta}, \code{dist_km}, \code{depth_m}, \code{variable}, \code{value}.
}
\description{
Long/tidy, one row per (station, depth bin, variable) — the shape both the
matrix builder and the climatology consume.
}
\details{
Depth is binned because CTD sensors sample continuously (47.283 m, 47.916 m,
…), so grouping by exact depth deduplicates almost nothing and the
native-resolution profile is jagged with sensor precision rather than signal.
}
\concept{transect}
