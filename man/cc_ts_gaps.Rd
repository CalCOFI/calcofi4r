% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{cc_ts_gaps}
\alias{cc_ts_gaps}
\title{Insert NA rows at time steps with no observations}
\usage{
cc_ts_gaps(d, ts_res)
}
\arguments{
\item{d}{data frame from \code{\link[=prep_ts_sp]{prep_ts_sp()}}: \code{time}, \code{name}, \code{avg}, \code{std}, \code{n},
\code{upr}, \code{lwr}.}

\item{ts_res}{temporal resolution, as passed to \code{\link[=prep_ts_sp]{prep_ts_sp()}}.}
}
\value{
\code{d} with NA-valued rows added at missing steps, ordered by name/time.
}
\description{
So a line chart BREAKS at an unsampled step instead of drawing straight
through it.
}
\details{
Highcharts connects consecutive points, and a species series is mostly zeros,
so an unsampled stretch renders as a flat line along zero — which reads as
"we looked and found none" when the truth is "nobody looked". Those are
different facts and the chart was showing the wrong one.

\code{cdfw_dungeness-crab} is the worked example: its sorted-archive effort exists
in only nine years (1984, 1988, 1998, 2004-2009), because the sorting log
records which archived jars have been examined and most have not. The chart
drew a continuous zero from 1984 to 2008, asserting measured absence across
roughly 20 years in which not one jar was opened.

Gaps are NA on \code{avg}/\code{std}/\code{upr}/\code{lwr} and \code{n = 0}, never \code{0} — zero is a
measurement, and collapsing the two is the bug. \code{n} tells them apart.

Only applied to resolutions that are a real time AXIS. \code{"quarter"}/\code{"month"}/
\code{"day"} are climatology CYCLES — every bin is populated by construction and a
gap there means something different — so they are left alone.

Exported because \code{db-viz-hex} carries its own copy of \code{\link[=prep_ts_sp]{prep_ts_sp()}}; both call
this, so the rule cannot drift into two versions.
}
\concept{analyze}
