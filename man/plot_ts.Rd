% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{plot_ts}
\alias{plot_ts}
\title{Create Dual-Panel Time Series Plot}
\usage{
plot_ts(sp_ts, env_ts, ts_res, sel_env_var)
}
\arguments{
\item{sp_ts}{data.frame from \code{\link{prep_ts_sp}} with columns: \code{time}, \code{name}, \code{avg}, \code{std}, \code{lwr}, \code{upr}}

\item{env_ts}{data.frame from \code{\link{prep_ts_env}} with columns: \code{time}, \code{avg}, \code{std}, \code{lwr}, \code{upr}}

\item{ts_res}{Character string specifying temporal resolution: "year", "quarter", "month", "day", etc.}

\item{sel_env_var}{Character string of environmental variable column name (e.g., "t_deg_c")}
}
\value{
highchart object with dual y-axes, line + ribbon series, and zoom capabilities
}
\description{
Generates an interactive Highcharts plot with species abundance and
environmental data in separate panels, with synchronized x-axis zoom and
resolution-dependent date formatting.
}
\details{
The function creates a two-panel plot with species on top and environmental
data on bottom. Date formatting adapts to \code{ts_res}. Standard error ribbons
are displayed as \code{arearange} series. Tooltips show values ± standard error.
}
\examples{
\dontrun{
sp_ts <- prep_ts_sp(df_sp, ts_res = "year")
env_ts <- prep_ts_env(df_env, ts_res = "year")
plot_ts(sp_ts, env_ts, ts_res = "year", sel_env_var = "t_deg_c")
}

}
\seealso{
\code{\link{prep_ts_sp}} for species time series data

\code{\link{prep_ts_env}} for environmental time series data
}
